<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            background-color: #f5f5f5;
            color: #333;
            line-height: 1.4;
        }
        .container {
            max-width: 650px;
            margin: 0 auto;
            background-color: white;
        }
        .header {
            background: linear-gradient(135deg, #7c69ef 0%, #a78bfa 100%);
            color: white;
            padding: 35px 30px;
            text-align: center;
        }
        .header h1 {
            font-size: 26px;
            font-weight: 600;
            margin-bottom: 6px;
        }
        .header .date {
            font-size: 15px;
            opacity: 0.95;
        }
        .content {
            padding: 25px 30px;
        }
        .metric {
            margin-bottom: 12px;
            font-size: 16px;
        }
        .metric-label {
            color: #333;
            margin-right: 8px;
        }
        .metric-value {
            font-weight: 700;
            font-size: 18px;
        }
        .value-red { color: #dc3545; }
        .value-green { color: {{ '#28a745' if reste >= 0 else '#dc3545' }}; }
        .value-default { color: #333; }
        .progress-section {
            margin: 15px 0;
        }
        .progress-bar-container {
            background-color: #e9ecef;
            border-radius: 6px;
            height: 28px;
            position: relative;
            width: 100%;
            border: 1px solid #ddd;
            overflow: visible;
        }
        .progress-bar-fill {
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: flex-start;
            padding-left: 10px;
            color: white;
            font-weight: 600;
            font-size: 13px;
            transition: width 0.3s ease;
            border-radius: 5px 0 0 5px;
        }
        .progress-marker-100 {
            position: absolute;
            right: -2px;
            top: 0;
            bottom: 0;
            width: 3px;
            background-color: #333;
            z-index: 10;
        }
        .progress-marker-100::before {
            content: '100%';
            position: absolute;
            right: -22px;
            top: -24px;
            font-size: 11px;
            font-weight: 700;
            color: #333;
            white-space: nowrap;
        }
        .progress-marker-time {
            position: absolute;
            top: 0;
            bottom: 0;
            width: 2px;
            background-color: #fd7e14;
            z-index: 5;
        }
        .progress-marker-time .marker-label {
            position: absolute;
            left: -35px;
            bottom: -24px;
            font-size: 11px;
            font-weight: 600;
            color: #fd7e14;
            white-space: nowrap;
        }
        .prediction-box {
            margin: 20px 0;
            padding: 15px 20px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-left: 4px solid;
            border-radius: 6px;
        }
        .prediction-title {
            font-size: 14px;
            font-weight: 600;
            color: #495057;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .prediction-value {
            font-size: 22px;
            font-weight: 700;
            margin: 8px 0;
        }
        .prediction-message {
            font-size: 13px;
            font-weight: 500;
            margin-top: 5px;
        }
        .prediction-detail {
            font-size: 12px;
            color: #6c757d;
            margin-top: 8px;
        }
        .families-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        .families-table th {
            background-color: #f8f9fa;
            padding: 12px 10px;
            text-align: left;
            font-size: 13px;
            font-weight: 600;
            color: #495057;
            border-bottom: 2px solid #dee2e6;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .families-table td {
            padding: 12px 10px;
            font-size: 14px;
            border-bottom: 1px solid #f0f0f0;
        }
        .families-table tr:hover {
            background-color: #fafafa;
        }
        .family-name {
            font-weight: 600;
            color: #333;
        }
        .amount-cell {
            color: #dc3545;
            font-weight: 700;
            text-align: right;
            white-space: nowrap;
        }
        .link-cell {
            text-align: center;
        }
        .btn-detail {
            display: inline-block;
            padding: 8px 16px;
            background-color: #7c69ef;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            font-size: 13px;
            font-weight: 500;
        }
        .btn-detail:hover {
            background-color: #5a4bbd;
        }
        .total-row {
            background-color: #f8f9fa;
            font-weight: 700;
            border-top: 2px solid #dee2e6;
        }
        .cta-section {
            text-align: center;
            margin: 30px 0;
            padding: 25px;
            background-color: #fff9e6;
            border-radius: 8px;
            border: 2px solid #ffc107;
        }
        .cta-section p {
            font-size: 15px;
            color: #856404;
            margin-bottom: 15px;
        }
        .btn-primary {
            display: inline-block;
            padding: 12px 30px;
            background-color: #7c69ef;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 600;
            margin-top: 10px;
        }
        .btn-primary:hover {
            background-color: #5a4bbd;
        }
        .footer {
            text-align: center;
            padding: 20px;
            background-color: #f8f9fa;
            font-size: 12px;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>💰 Rapport Linxo - Budget</h1>
            <div class="date">{{ report_date }}</div>
        </div>

        <div class="content">
            <div class="metric">
                <a href="{{ frais_fixes_url }}" style="text-decoration: none; color: inherit; border-bottom: 1px dashed #007bff;">
                    <span class="metric-label">Frais fixes 🔗</span>
                </a>
                <span class="metric-value value-red">{{ "%.2f"|format(total_fixes) }} €</span>
                <span class="metric-label">/ {{ budget_fixes_prevu }} €</span>
            </div>

            <div class="progress-section">
                <div class="progress-bar-container">
                    <div class="progress-bar-fill" style="width: {{ pourcentage_fixes }}%; background: linear-gradient(90deg, {{ couleur_barre_fixes }} 0%, {{ couleur_barre_fixes }}dd 100%);">
                        {{ "%.0f"|format(pourcentage_fixes) }}%
                    </div>
                    <div class="progress-marker-100"></div>
                    <div class="progress-marker-time" style="left: {{ "%.1f"|format(avancement_mois) }}%;">
                        <span class="marker-label">Aujourd'hui</span>
                    </div>
                </div>
            </div>

            <div class="metric">
                <a href="{{ depenses_variables_url }}" style="text-decoration: none; color: inherit; border-bottom: 1px dashed #007bff;">
                    <span class="metric-label">Dépenses variables 🔗</span>
                </a>
                <span class="metric-value value-red">{{ "%.2f"|format(total_variables) }} €</span>
                <span class="metric-label">/ {{ budget_max }} €</span>
            </div>

            <div class="progress-section">
                <div class="progress-bar-container">
                    <div class="progress-bar-fill" style="width: {{ pourcentage }}%; background: linear-gradient(90deg, {{ couleur_barre_variables }} 0%, {{ couleur_barre_variables }}dd 100%);">
                        {{ "%.0f"|format(pourcentage) }}%
                    </div>
                    <div class="progress-marker-100"></div>
                    <div class="progress-marker-time" style="left: {{ "%.1f"|format(avancement_mois) }}%;">
                        <span class="marker-label">Aujourd'hui</span>
                    </div>
                </div>
            </div>

            <div class="metric" style="margin-top: 15px;">
                <span class="metric-label">{{ "Dépassement" if reste < 0 else "Reste disponible" }}</span>
                <span class="metric-value value-green">{{ "%.2f"|format(reste if reste >= 0 else -reste) }} €</span>
            </div>

            <div class="prediction-box" style="border-left-color: {{ couleur_prediction }};">
                <div class="prediction-title">
                    🔮 Prédiction fin de mois
                </div>
                <div class="prediction-value" style="color: {{ couleur_prediction }};">
                    {{ "%.2f"|format(prediction_fin_mois) }} €
                </div>
                <div class="prediction-message" style="color: {{ couleur_prediction }};">
                    {{ message_prediction }}
                </div>
                <div class="prediction-detail">
                    📊 Basé sur {{ jours_restants }} jour{{ "s" if jours_restants > 1 else "" }} restant{{ "s" if jours_restants > 1 else "" }} dans le mois
                </div>
            </div>

            <h2 style="font-size: 20px; margin: 25px 0 15px 0; color: #333;">Dépenses par famille</h2>

            <table class="families-table">
                <thead>
                    <tr>
                        <th>Famille</th>
                        <th style="text-align: right;">Montant</th>
                        <th style="text-align: center;">Détail</th>
                    </tr>
                </thead>
                <tbody>
                    {% for family in families %}
                    <tr>
                        <td class="family-name">{{ family.name }}</td>
                        <td class="amount-cell">{{ "%.2f"|format(family.total) }} €</td>
                        <td class="link-cell">
                            <a href="{{ family.url }}" class="btn-detail">Voir →</a>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
                <tfoot>
                    <tr class="total-row">
                        <td><strong>TOTAL</strong></td>
                        <td class="amount-cell"><strong>{{ "%.2f"|format(grand_total) }} €</strong></td>
                        <td></td>
                    </tr>
                </tfoot>
            </table>

            <div class="cta-section">
                <p><strong>📊 Consultez le rapport complet en ligne</strong></p>
                <p>Accédez à tous les détails de vos dépenses par famille</p>
                <a href="{{ index_url }}" class="btn-primary">Voir le rapport complet</a>
            </div>
        </div>

        <div class="footer">
            🤖 Rapport généré automatiquement par votre Agent Linxo
            <div style="margin-top: 10px;">
                <a href="{{ admin_url }}" style="text-decoration: none; color: #666; font-size: 18px; opacity: 0.7; transition: opacity 0.2s;" title="Administration">
                    ⚙️
                </a>
            </div>
        </div>
    </div>
</body>
</html>
